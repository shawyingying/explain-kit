---
description: 给当前静态 HTML 原型 demo 加「需求讲解」遮罩层（聚光灯高亮 + 步骤讲解，讲解卡可拖动）
globs: ["**/*.html"]
alwaysApply: false
---

当用户要求给当前原型 demo 加「需求讲解」时，按以下流程（**全程在原 demo 的副本里操作，原 demo 不动**）：

1. **定位 demo 根**：找到含 `.html` 的目录作为 demo 根——可能就是当前目录，也可能是子文件夹（如 `demo/`）。需求文档可能在兄弟文件夹（如 `需求文档/`）。
2. **复制 demo 到同级副本**：在 demo 根同级建 `<原名>-讲解/`，整份 `cp -R` 复制 demo 根进去。单文件 demo（一个 `.html` 躺在大杂烩文件夹里）只复制该 `.html` + 它引用的本地资源（`<link>/<script>/<img>/<source>/<video>/<audio>` 本地路径，不含 `http(s)://`、`data:`），保持目录结构；原代码绝对路径（`/assets/...`、`file:///`、`C:\...`）在副本里改相对，避免引用失效。后续都在副本里。
3. 读 explain-kit 的 `PLAYBOOK.md` 并按其流程操作：
   - 全局装过 skill 时位于 `~/.claude/skills/explain/PLAYBOOK.md`（或 `~/.codex/skills/explain/PLAYBOOK.md`）；
   - 否则从克隆的 explain-kit 仓库根读取 `PLAYBOOK.md`。
4. 把 `engine/` 拷到**副本根**（与 `.html` 同级）。
5. 给副本里涉及的每个 `.html` 加 3 行 include（`engine/explain.css` + `engine/explain-engine.js` + `explain-config.js`，相对路径），不动副本里的原 demo 内容。
6. 基于 `explain-config.template.js` 在副本根生成 `explain-config.js`（需求→steps；`Explain.registerAction` / `registerCleanup` / `registerExit`；每步 setup 自包含）。
7. **需求文档**：若用户没给，先问用户要（路径/粘贴/按 demo 推断），不要臆造；多份文档时问「逐个生成」还是「一次性生成」并给利弊，建议默认逐个跑通第一条再批量。
8. 告诉用户用浏览器打开**副本**里的 `.html` 验证：右下角「📖 需求讲解」→ 选需求卡片 → 走右侧步骤点；讲解卡挡内容时可拖标题栏移开。

> `engine/` 与 `PLAYBOOK.md` 是单一真相源，与 explain skill 共用。
