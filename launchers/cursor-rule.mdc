---
description: 给当前静态 HTML 原型 demo 加「需求讲解」遮罩层（聚光灯高亮 + 步骤讲解）
globs: ["**/*.html"]
alwaysApply: false
---

当用户要求给当前原型 demo 加「需求讲解」时，按以下流程：

1. 读 explain-kit 的 `PLAYBOOK.md` 并按其流程操作。
   - 全局安装过 Claude Code 的 explain skill 时位于 `~/.claude/skills/explain/PLAYBOOK.md`；
   - 否则从克隆的 explain-kit 仓库根读取 `PLAYBOOK.md`。
2. 把 `engine/`（来自 `~/.claude/skills/explain/engine/` 或仓库 `engine/`）拷到 demo 项目根。
3. 给讲解涉及的每个 `.html` 加 3 行 include（`engine/explain.css` + `engine/explain-engine.js` + `explain-config.js`），不改原 demo 既有内容。
4. 基于 `explain-config.template.js` 生成 `explain-config.js`（需求→steps；`Explain.registerAction` / `registerCleanup` / `registerExit`；每步 setup 自包含）。
5. 告诉用户用浏览器验证：右下角「📖 需求讲解」→ 选需求卡片 → 走右侧步骤点。

> Cursor 没有 skill 机制，本规则是 auto-attached stub。`engine/` 与 `PLAYBOOK.md` 是单一真相源，与 Claude Code 的 explain skill 共用。
